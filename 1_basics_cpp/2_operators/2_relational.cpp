#include<iostream>
using namespace std;
int main(){
    cout << "Relational Operator : " << endl;
    int a = 10  , b = 10 , c = 20;
    cout << "relational operators return true (1) or false (0)" << endl;
    cout << "if a is greater than b : (a > b) = " << (a > b) << endl;
    cout << "if a is equal to b : (a == b) = " << (a == b) << endl;
    cout << "if a is smaller than b : (a < b) = " << (a < b) << endl;
    cout << "if a is greater than or equal to b : (a >= b) = " << (a >= b) << endl;
    cout << "if a is less than or equal to b : (a <= b) = " << (a <= b) << endl;
    cout << "if a is not equal to b : (a != b) = " << (a != b) << endl;
    return 0;
}